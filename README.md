# Great Nexus - Backend

Node.js + Express + MongoDB backend para Great Nexus.

## Setup
1. Copiar `.env.example` para `.env` e preencher variáveis.
2. `npm install`
3. `npm run dev` (ou `npm start`)

Endpoints principais:
- POST /api/v1/auth/login
- POST /api/v1/auth/register
- GET /api/v1/erp/sales/invoices
- POST /api/v1/erp/sales/invoices
